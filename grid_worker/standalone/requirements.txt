# Core data + system deps (always required)
polars>=0.20.0
numpy>=1.24.0
pandas>=2.0.0
psutil>=5.9.0
pyyaml>=6.0
requests>=2.28.0
yfinance>=0.2.0

# ML deps for ml_train, optimization, walk_forward, alpha_factory
# (all CPU-capable; xgboost auto-uses GPU at runtime if torch+CUDA is present)
xgboost>=2.0.0
lightgbm>=4.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
optuna>=3.4.0

# IBKR — only needed if customer wires their own broker subscription locally
ib_async>=1.0.0

# NOTE: torch is NOT listed here. CUDA torch (~2 GB) is a separate consent-gated
# download triggered from Electron's gpu_setup.js — see TORCH_WHEEL_INDEX.
# CPU-only torch is not auto-installed either; users who decline GPU keep the
# CPU lane running on xgboost/lightgbm/sklearn which all work without torch.
